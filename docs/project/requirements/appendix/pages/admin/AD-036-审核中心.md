# AD-036 审核中心

> 文档定位：逐页查询附录。一级需求范围、状态汇总和最终验收以所属功能模块文档为准。

| 项目 | 内容 |
|---|---|
| 需求ID | AD-036 |
| 所属模块 | [查看模块需求](../../../modules/M17-平台审核、合规与安全治理.md) |
| 所属端 | 平台后台 |
| 阶段 | P0 |
| 业务域 | 平台审核合规 |
| 路由 | `admin/admin-review.html` |
| 当前状态 | 未开始 |
| 实现与验收证据 | — |
| 已知缺口 | — |

## 完整页面需求


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-review.html` |
| 业务域 | 平台审核合规 |
| 阶段 | P0 |
| 主要用户 | 内容审核员、合规/法务 |

### 页面目标

分配和处理文本、图片、视频、模板和商单内容审核，记录规则、证据与决定。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `审核中心` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：执行或查看合规检查
  - 操作：补充证据与授权
  - 操作：审批、阻断或申诉
  - 字段：检查/授权 ID
  - 字段：规则版本
  - 字段：风险等级
  - 字段：证据
  - 字段：责任人
  - 字段：处置状态
  - 字段：有效期
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ComplianceCheck`
  - `ComplianceRecord`
  - `RightsRecord`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.compliance.view`
  - `admin.compliance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：显示规则版本和最近检查时间。
  - 阻断态：高风险项显示证据、责任人、解除条件和申诉入口。
  - 通过态：绑定不可变项目版本；项目内容变化后旧结果失效。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/compliance`
  - `POST /api/v1/admin/compliance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 内容审核员、合规/法务 能从规定入口进入 `审核中心`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
