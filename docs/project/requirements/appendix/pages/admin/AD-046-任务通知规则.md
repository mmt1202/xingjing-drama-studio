# AD-046 任务通知规则

> 文档定位：逐页查询附录。一级需求范围、状态汇总和最终验收以所属功能模块文档为准。

| 项目 | 内容 |
|---|---|
| 需求ID | AD-046 |
| 所属模块 | [查看模块需求](../../../modules/M18-平台运维、客服、消息与运营配置.md) |
| 所属端 | 平台后台 |
| 阶段 | P1 |
| 业务域 | 平台消息 |
| 路由 | `admin/admin-task-notice-rules.html` |
| 当前状态 | 未开始 |
| 实现与验收证据 | — |
| 已知缺口 | — |

## 完整页面需求


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-task-notice-rules.html` |
| 业务域 | 平台消息 |
| 阶段 | P1 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

配置任务失败、长时间排队、扣费异常、导出完成的通知策略。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `任务通知规则` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：筛选消息和发送记录
  - 操作：编辑模板或规则
  - 操作：预览、测试并发布
  - 字段：模板/通知 ID
  - 字段：渠道
  - 字段：受众
  - 字段：变量
  - 字段：发送策略
  - 字段：送达状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Notification`
  - `MessageTemplate`
  - `DeliveryLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.notification.view`
  - `admin.notification.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/notifications`
  - `POST /api/v1/admin/notifications/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `任务通知规则`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
