# M04 主体与素材资产需求设计

- 范围：主体与素材资产
- 页面覆盖：5 项（P0=1，P1=2，P2=2）
- 完成度：0/5 已验证
- 当前状态：开发中
- 联合验收包：A3 资产准备闭环

- 当前实现证据：EV-M04-M05-IMPL-001、EV-M04-M05-TEST-001、EV-M04-PERSISTENCE-001、EV-M04-MIGRATION-002、EV-M04-RUNTIME-003、EV-M04-M05-FRONTEND-AUTH-004
- 状态说明：页面组件、typed API、资产 SQLAlchemy 仓储、正式迁移和主应用 API 接线已存在；资产创建、版本、恢复、引用与审计走可信会话和数据库。AI 提取/三视图/表情集、授权证据对象存储、素材市场、真实环境联调、用户验收及 A3 联合验收仍缺，模块保持开发中。

## 1. 模块目标与边界

本模块统一承载「主体与素材资产」相关需求。模块内页面、接口、数据、权限、异常与验收必须作为同一能力集合维护，不允许只完成可见页面而遗漏服务端规则或失败路径。

- 前置依赖：M02、M03、对象存储、版本能力
- 下游能力：分镜引用、图片与视频生成
- 页面只做范围映射；模块完成必须通过联合验收，不以单页可打开为完成。

## 2. 功能范围与完成状态

| 需求ID | 功能/页面 | 阶段 | 状态 | 实现与验收证据 | 已知缺口 |
|---|---|---|---|---|---|
| CR-045 | 全局资产库 | P0 | 未开始 | — | — |
| CR-010 | 角色三视图 | P1 | 未开始 | — | — |
| CR-034 | 角色表情集 | P1 | 未开始 | — | — |
| CR-005 | 素材详情 | P2 | 未开始 | — | — |
| CR-006 | 素材市场 | P2 | 未开始 | — | — |

## 3. 数据、权限、接口与测试覆盖

| 需求ID | 主要对象 | 权限 | API | 测试 |
|---|---|---|---|---|
| CR-045 | Asset<br>AssetVersion<br>RightsRecord<br>AssetReference | asset.view<br>asset.manage | GET /api/v1/projects/{projectId}/assets<br>POST /api/v1/projects/{projectId}/assets/actions | TC-CR-045-01..06 |
| CR-010 | Asset<br>AssetVersion<br>RightsRecord<br>AssetReference | asset.view<br>asset.manage | GET /api/v1/projects/{projectId}/assets<br>POST /api/v1/projects/{projectId}/assets/actions | TC-CR-010-01..06 |
| CR-034 | Asset<br>AssetVersion<br>RightsRecord<br>AssetReference | asset.view<br>asset.manage | GET /api/v1/projects/{projectId}/assets<br>POST /api/v1/projects/{projectId}/assets/actions | TC-CR-034-01..06 |
| CR-005 | Asset<br>AssetVersion<br>RightsRecord<br>AssetReference | asset.view<br>asset.manage | GET /api/v1/projects/{projectId}/assets<br>POST /api/v1/projects/{projectId}/assets/actions | TC-CR-005-01..06 |
| CR-006 | Asset<br>AssetVersion<br>RightsRecord<br>AssetReference | asset.view<br>asset.manage | GET /api/v1/projects/{projectId}/assets<br>POST /api/v1/projects/{projectId}/assets/actions | TC-CR-006-01..06 |

## 4. 联合验收要求

本模块并入「A3 资产准备闭环」验收，不单独以页面数量结项。验收至少覆盖：

1. 前置依赖真实可用，权限和租户上下文贯穿全流程；
2. 模块内数据对象、状态机、API 和页面展示一致；
3. 正常、空、失败、冲突、取消、超时、重试和恢复路径按适用范围通过；
4. 模块产生的任务、费用、审计、合规和版本证据可以追踪；
5. 下游模块能够消费本模块产物，刷新或重复请求不产生重复副作用。

## 5. 详细功能需求

### CR-045 全局资产库

- 系统：创作者
- 路由：`creator/global-assets.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/global-assets.html` |
| 业务域 | 主体与素材资产 |
| 阶段 | P0 |
| 主要用户 | 视觉师、导演、资产管理员 |

### 页面目标

在工作区范围复用角色、场景、道具、音色、风格和授权资产，并查看项目引用。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `全局资产库` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：浏览和筛选资产
  - 操作：创建/生成资产版本
  - 操作：绑定引用或授权
  - 字段：资产 ID
  - 字段：资产类型
  - 字段：名称/标签
  - 字段：来源
  - 字段：授权状态
  - 字段：版本
  - 字段：引用次数
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Asset`
  - `AssetVersion`
  - `RightsRecord`
  - `AssetReference`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `asset.view`
  - `asset.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/assets`
  - `POST /api/v1/projects/{projectId}/assets/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 视觉师、导演、资产管理员 能从规定入口进入 `全局资产库`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-010 角色三视图

- 系统：创作者
- 路由：`creator/character-turnaround.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/character-turnaround.html` |
| 业务域 | 主体与素材资产 |
| 阶段 | P1 |
| 主要用户 | 视觉师、导演、资产管理员 |

### 页面目标

生成正面、侧面、背面、半身、全身和服装变化，支撑角色一致性。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `角色三视图` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：浏览和筛选资产
  - 操作：创建/生成资产版本
  - 操作：绑定引用或授权
  - 字段：资产 ID
  - 字段：资产类型
  - 字段：名称/标签
  - 字段：来源
  - 字段：授权状态
  - 字段：版本
  - 字段：引用次数
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Asset`
  - `AssetVersion`
  - `RightsRecord`
  - `AssetReference`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `asset.view`
  - `asset.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/assets`
  - `POST /api/v1/projects/{projectId}/assets/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 视觉师、导演、资产管理员 能从规定入口进入 `角色三视图`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-034 角色表情集

- 系统：创作者
- 路由：`creator/expression-set.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/expression-set.html` |
| 业务域 | 主体与素材资产 |
| 阶段 | P1 |
| 主要用户 | 视觉师、导演、资产管理员 |

### 页面目标

生成喜怒哀乐、惊讶、压抑、哭戏、轻蔑等表情资产。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `角色表情集` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：浏览和筛选资产
  - 操作：创建/生成资产版本
  - 操作：绑定引用或授权
  - 字段：资产 ID
  - 字段：资产类型
  - 字段：名称/标签
  - 字段：来源
  - 字段：授权状态
  - 字段：版本
  - 字段：引用次数
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Asset`
  - `AssetVersion`
  - `RightsRecord`
  - `AssetReference`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `asset.view`
  - `asset.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/assets`
  - `POST /api/v1/projects/{projectId}/assets/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 视觉师、导演、资产管理员 能从规定入口进入 `角色表情集`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-005 素材详情

- 系统：创作者
- 路由：`creator/asset-market-detail.html`
- 阶段：P2
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/asset-market-detail.html` |
| 业务域 | 主体与素材资产 |
| 阶段 | P2 |
| 主要用户 | 视觉师、导演、资产管理员 |

### 页面目标

查看素材预览、授权范围、价格、使用记录和加入资产库入口。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `素材详情` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：浏览和筛选资产
  - 操作：创建/生成资产版本
  - 操作：绑定引用或授权
  - 操作：查看对象完整详情与关联记录
  - 字段：资产 ID
  - 字段：资产类型
  - 字段：名称/标签
  - 字段：来源
  - 字段：授权状态
  - 字段：版本
  - 字段：引用次数
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Asset`
  - `AssetVersion`
  - `RightsRecord`
  - `AssetReference`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `asset.view`
  - `asset.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/assets`
  - `POST /api/v1/projects/{projectId}/assets/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 视觉师、导演、资产管理员 能从规定入口进入 `素材详情`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-006 素材市场

- 系统：创作者
- 路由：`creator/asset-market.html`
- 阶段：P2
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/asset-market.html` |
| 业务域 | 主体与素材资产 |
| 阶段 | P2 |
| 主要用户 | 视觉师、导演、资产管理员 |

### 页面目标

浏览角色、场景、道具、音乐、音效、封面模板等可商用素材。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `素材市场` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：浏览和筛选资产
  - 操作：创建/生成资产版本
  - 操作：绑定引用或授权
  - 字段：资产 ID
  - 字段：资产类型
  - 字段：名称/标签
  - 字段：来源
  - 字段：授权状态
  - 字段：版本
  - 字段：引用次数
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Asset`
  - `AssetVersion`
  - `RightsRecord`
  - `AssetReference`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `asset.view`
  - `asset.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/assets`
  - `POST /api/v1/projects/{projectId}/assets/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 视觉师、导演、资产管理员 能从规定入口进入 `素材市场`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
## 6. 本模块不单独判定完成的事项

- 只有静态页面、原型或 mock 数据；
- 只有接口或数据库，没有可用交互和错误恢复；
- 只有单页测试，没有前置依赖与下游消费验证；
- 缺少实现、自动化测试、运行证据或适用发布门禁。
