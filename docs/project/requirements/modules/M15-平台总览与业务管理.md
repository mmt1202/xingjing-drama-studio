# M15 平台总览与业务管理需求设计

- 范围：平台总览、平台业务管理
- 页面覆盖：6 项（P0=5，P1=1，P2=0）
- 完成度：0/6 已验证
- 当前状态：未开始
- 联合验收包：D1 平台业务治理闭环

## 1. 模块目标与边界

本模块统一承载「平台总览与业务管理」相关需求。模块内页面、接口、数据、权限、异常与验收必须作为同一能力集合维护，不允许只完成可见页面而遗漏服务端规则或失败路径。

- 前置依赖：平台共享地基、审计、各业务服务
- 下游能力：用户、项目、任务和内容治理
- 页面只做范围映射；模块完成必须通过联合验收，不以单页可打开为完成。

## 2. 功能范围与完成状态

| 需求ID | 功能/页面 | 阶段 | 状态 | 实现与验收证据 | 已知缺口 |
|---|---|---|---|---|---|
| AD-011 | 内容管理 | P0 | 未开始 | — | — |
| AD-017 | 平台首页 | P0 | 未开始 | — | — |
| AD-030 | 项目管理 | P0 | 未开始 | — | — |
| AD-047 | 生成任务 | P0 | 未开始 | — | — |
| AD-050 | 用户管理 | P0 | 未开始 | — | — |
| AD-048 | 团队管理 | P1 | 未开始 | — | — |

## 3. 数据、权限、接口与测试覆盖

| 需求ID | 主要对象 | 权限 | API | 测试 |
|---|---|---|---|---|
| AD-011 | User<br>Workspace<br>Project<br>GenerationTask<br>AuditLog | admin.business.view<br>admin.business.manage | GET /api/v1/admin/business-objects<br>POST /api/v1/admin/business-objects/actions | TC-AD-011-01..06 |
| AD-017 | AnalyticsEvent<br>MetricSnapshot | admin.business.view<br>admin.business.manage | GET /api/v1/admin/business-objects<br>POST /api/v1/admin/business-objects/actions | TC-AD-017-01..06 |
| AD-030 | User<br>Workspace<br>Project<br>GenerationTask<br>AuditLog | admin.business.view<br>admin.business.manage | GET /api/v1/admin/business-objects<br>POST /api/v1/admin/business-objects/actions | TC-AD-030-01..06 |
| AD-047 | User<br>Workspace<br>Project<br>GenerationTask<br>AuditLog | admin.business.view<br>admin.business.manage | GET /api/v1/admin/business-objects<br>POST /api/v1/admin/business-objects/actions | TC-AD-047-01..06 |
| AD-050 | User<br>Workspace<br>Project<br>GenerationTask<br>AuditLog | admin.business.view<br>admin.business.manage | GET /api/v1/admin/business-objects<br>POST /api/v1/admin/business-objects/actions | TC-AD-050-01..06 |
| AD-048 | User<br>Workspace<br>Project<br>GenerationTask<br>AuditLog | admin.business.view<br>admin.business.manage | GET /api/v1/admin/business-objects<br>POST /api/v1/admin/business-objects/actions | TC-AD-048-01..06 |

## 4. 联合验收要求

本模块并入「D1 平台业务治理闭环」验收，不单独以页面数量结项。验收至少覆盖：

1. 前置依赖真实可用，权限和租户上下文贯穿全流程；
2. 模块内数据对象、状态机、API 和页面展示一致；
3. 正常、空、失败、冲突、取消、超时、重试和恢复路径按适用范围通过；
4. 模块产生的任务、费用、审计、合规和版本证据可以追踪；
5. 下游模块能够消费本模块产物，刷新或重复请求不产生重复副作用。

## 5. 详细功能需求

### AD-011 内容管理

- 系统：平台后台
- 路由：`admin/admin-content.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-content.html` |
| 业务域 | 平台业务管理 |
| 阶段 | P0 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

按用户、团队、项目和内容类型查询剧本、图片、视频、音频和成片，执行受控治理。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `内容管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看内容管理
  - 操作：更新内容管理 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `User`
  - `Workspace`
  - `Project`
  - `GenerationTask`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/business-objects`
  - `POST /api/v1/admin/business-objects/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `内容管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-017 平台首页

- 系统：平台后台
- 路由：`admin/admin-home.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-home.html` |
| 业务域 | 平台总览 |
| 阶段 | P0 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

展示平台用户、项目、任务、模型、收入、审核、工单和服务健康的运营总览。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `平台首页` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看平台首页
  - 操作：更新平台首页 配置
  - 操作：保存并审计变更
  - 操作：切换时间范围和统计维度
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `AnalyticsEvent`
  - `MetricSnapshot`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/business-objects`
  - `POST /api/v1/admin/business-objects/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `平台首页`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-030 项目管理

- 系统：平台后台
- 路由：`admin/admin-projects.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-projects.html` |
| 业务域 | 平台业务管理 |
| 阶段 | P0 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

跨租户按权限查询项目、状态、任务、成本和风险，执行冻结等受控平台动作。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `项目管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看项目管理
  - 操作：更新项目管理 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `User`
  - `Workspace`
  - `Project`
  - `GenerationTask`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/business-objects`
  - `POST /api/v1/admin/business-objects/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `项目管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-047 生成任务

- 系统：平台后台
- 路由：`admin/admin-tasks.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-tasks.html` |
| 业务域 | 平台业务管理 |
| 阶段 | P0 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

跨租户检索生成和导出任务，查看尝试、模型、成本、失败原因并执行受控补偿。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `生成任务` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看生成任务
  - 操作：更新生成任务 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `User`
  - `Workspace`
  - `Project`
  - `GenerationTask`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/business-objects`
  - `POST /api/v1/admin/business-objects/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `生成任务`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-050 用户管理

- 系统：平台后台
- 路由：`admin/admin-users.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-users.html` |
| 业务域 | 平台业务管理 |
| 阶段 | P0 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

管理用户状态、工作区、项目、订单、风险和工单，在审批下执行封禁或恢复。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `用户管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看用户管理
  - 操作：更新用户管理 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `User`
  - `Workspace`
  - `Project`
  - `GenerationTask`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/business-objects`
  - `POST /api/v1/admin/business-objects/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `用户管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-048 团队管理

- 系统：平台后台
- 路由：`admin/admin-teams.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-teams.html` |
| 业务域 | 平台业务管理 |
| 阶段 | P1 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

管理团队状态、套餐、成员规模、项目、成本、企业认证和风险标记。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `团队管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看团队管理
  - 操作：更新团队管理 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `User`
  - `Workspace`
  - `Project`
  - `GenerationTask`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/business-objects`
  - `POST /api/v1/admin/business-objects/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `团队管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
## 6. 本模块不单独判定完成的事项

- 只有静态页面、原型或 mock 数据；
- 只有接口或数据库，没有可用交互和错误恢复；
- 只有单页测试，没有前置依赖与下游消费验证；
- 缺少实现、自动化测试、运行证据或适用发布门禁。
