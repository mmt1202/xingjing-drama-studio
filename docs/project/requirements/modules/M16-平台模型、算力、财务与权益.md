# M16 平台模型、算力、财务与权益需求设计

- 范围：平台模型运营、平台财务与权益、算力与成本
- 页面覆盖：14 项（P0=3，P1=11，P2=0）
- 完成度：0/14 已验证
- 当前状态：未开始
- 联合验收包：D2 模型算力财务闭环

## 1. 模块目标与边界

本模块统一承载「平台模型、算力、财务与权益」相关需求。模块内页面、接口、数据、权限、异常与验收必须作为同一能力集合维护，不允许只完成可见页面而遗漏服务端规则或失败路径。

- 前置依赖：任务、模型供应商、账务与审计
- 下游能力：生成路由、计费、订单和财务运营
- 页面只做范围映射；模块完成必须通过联合验收，不以单页可打开为完成。

## 2. 功能范围与完成状态

| 需求ID | 功能/页面 | 阶段 | 状态 | 实现与验收证据 | 已知缺口 |
|---|---|---|---|---|---|
| AD-004 | 算力计费 | P0 | 未开始 | — | — |
| AD-023 | 模型管理 | P0 | 未开始 | — | — |
| CR-009 | 算力账单 | P0 | 未开始 | — | — |
| AD-012 | 成本看板 | P1 | 未开始 | — | — |
| AD-013 | 权益管理 | P1 | 未开始 | — | — |
| AD-018 | 发票管理 | P1 | 未开始 | — | — |
| AD-021 | 模型回调日志 | P1 | 未开始 | — | — |
| AD-022 | 模型质量看板 | P1 | 未开始 | — | — |
| AD-026 | 订单管理 | P1 | 未开始 | — | — |
| AD-027 | 套餐详情 | P1 | 未开始 | — | — |
| AD-028 | 套餐管理 | P1 | 未开始 | — | — |
| AD-033 | 财务对账 | P1 | 未开始 | — | — |
| AD-034 | 退款管理 | P1 | 未开始 | — | — |
| AD-035 | 收入看板 | P1 | 未开始 | — | — |

## 3. 数据、权限、接口与测试覆盖

| 需求ID | 主要对象 | 权限 | API | 测试 |
|---|---|---|---|---|
| AD-004 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-004-01..06 |
| AD-023 | GenerationTask<br>ModelProvider<br>ModelDefinition<br>ModelCallRecord<br>GeneratedAsset | admin.model.view<br>admin.model.manage | GET /api/v1/admin/models<br>POST /api/v1/admin/models/actions | TC-AD-023-01..06 |
| CR-009 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | billing.view<br>billing.manage | GET /api/v1/workspaces/{workspaceId}/credit-account<br>GET /api/v1/workspaces/{workspaceId}/credit-transactions | TC-CR-009-01..06 |
| AD-012 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-012-01..06 |
| AD-013 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-013-01..06 |
| AD-018 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-018-01..06 |
| AD-021 | GenerationTask<br>ModelProvider<br>ModelDefinition<br>ModelCallRecord<br>GeneratedAsset | admin.model.view<br>admin.model.manage | GET /api/v1/admin/models<br>POST /api/v1/admin/models/actions | TC-AD-021-01..06 |
| AD-022 | GenerationTask<br>ModelProvider<br>ModelDefinition<br>ModelCallRecord<br>GeneratedAsset | admin.model.view<br>admin.model.manage | GET /api/v1/admin/models<br>POST /api/v1/admin/models/actions | TC-AD-022-01..06 |
| AD-026 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-026-01..06 |
| AD-027 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-027-01..06 |
| AD-028 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-028-01..06 |
| AD-033 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-033-01..06 |
| AD-034 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-034-01..06 |
| AD-035 | CreditAccount<br>CreditHold<br>CreditTransaction<br>Order<br>Invoice | admin.finance.view<br>admin.finance.manage | GET /api/v1/admin/finance<br>POST /api/v1/admin/finance/actions | TC-AD-035-01..06 |

## 4. 联合验收要求

本模块并入「D2 模型算力财务闭环」验收，不单独以页面数量结项。验收至少覆盖：

1. 前置依赖真实可用，权限和租户上下文贯穿全流程；
2. 模块内数据对象、状态机、API 和页面展示一致；
3. 正常、空、失败、冲突、取消、超时、重试和恢复路径按适用范围通过；
4. 模块产生的任务、费用、审计、合规和版本证据可以追踪；
5. 下游模块能够消费本模块产物，刷新或重复请求不产生重复副作用。

## 5. 详细功能需求

### AD-004 算力计费

- 系统：平台后台
- 路由：`admin/admin-billing.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-billing.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P0 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

管理平台算力账户、价格规则、冻结与结算异常、调账和成本核对。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `算力计费` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `算力计费`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-023 模型管理

- 系统：平台后台
- 路由：`admin/admin-models.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-models.html` |
| 业务域 | 平台模型运营 |
| 阶段 | P0 |
| 主要用户 | 模型运营、技术运维 |

### 页面目标

配置模型供应商、能力、价格、路由、配额、健康状态、回调和上下线。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `模型管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看模型能力和任务状态
  - 操作：提交/取消/重试任务
  - 操作：比较并选择生成结果
  - 字段：任务/模型 ID
  - 字段：能力类型
  - 字段：模型版本
  - 字段：输入摘要
  - 字段：进度/状态
  - 字段：预估与实际成本
  - 字段：失败原因
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `GenerationTask`
  - `ModelProvider`
  - `ModelDefinition`
  - `ModelCallRecord`
  - `GeneratedAsset`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.model.view`
  - `admin.model.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：读取任务、模型能力与余额；不得用缓存余额提交付费任务。
  - 处理中：queued/running/retrying 显示服务端进度、排队位置和取消能力。
  - 异常态：failed/timeout/cancelled 必须显示归一化原因、扣费结果和可执行恢复动作。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/models`
  - `POST /api/v1/admin/models/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 模型运营、技术运维 能从规定入口进入 `模型管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-009 算力账单

- 系统：创作者
- 路由：`creator/billing.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/billing.html` |
| 业务域 | 算力与成本 |
| 阶段 | P0 |
| 主要用户 | 项目负责人、团队财务 |

### 页面目标

查看当前工作区可用算力、冻结额、消费流水、充值记录及按项目拆分的成本。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `算力账单` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `billing.view`
  - `billing.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/credit-account`
  - `GET /api/v1/workspaces/{workspaceId}/credit-transactions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 项目负责人、团队财务 能从规定入口进入 `算力账单`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-012 成本看板

- 系统：平台后台
- 路由：`admin/admin-cost-dashboard.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-cost-dashboard.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

查看模型成本、存储成本、转码成本、补偿成本和毛利。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `成本看板` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 操作：切换时间范围和统计维度
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `成本看板`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-013 权益管理

- 系统：平台后台
- 路由：`admin/admin-entitlements.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-entitlements.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

管理功能权限、模型权限、导出额度、发布包额度和 API 调用额度。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `权益管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `权益管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-018 发票管理

- 系统：平台后台
- 路由：`admin/admin-invoice.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-invoice.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

管理企业开票、发票抬头、开票记录和异常处理。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `发票管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `发票管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-021 模型回调日志

- 系统：平台后台
- 路由：`admin/admin-model-callback-log.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-model-callback-log.html` |
| 业务域 | 平台模型运营 |
| 阶段 | P1 |
| 主要用户 | 模型运营、技术运维 |

### 页面目标

追踪第三方模型回调、签名、状态、失败原因和重放操作。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `模型回调日志` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看模型能力和任务状态
  - 操作：提交/取消/重试任务
  - 操作：比较并选择生成结果
  - 操作：按请求 ID、对象和操作者检索日志
  - 字段：任务/模型 ID
  - 字段：能力类型
  - 字段：模型版本
  - 字段：输入摘要
  - 字段：进度/状态
  - 字段：预估与实际成本
  - 字段：失败原因
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `GenerationTask`
  - `ModelProvider`
  - `ModelDefinition`
  - `ModelCallRecord`
  - `GeneratedAsset`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.model.view`
  - `admin.model.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：读取任务、模型能力与余额；不得用缓存余额提交付费任务。
  - 处理中：queued/running/retrying 显示服务端进度、排队位置和取消能力。
  - 异常态：failed/timeout/cancelled 必须显示归一化原因、扣费结果和可执行恢复动作。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/models`
  - `POST /api/v1/admin/models/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 模型运营、技术运维 能从规定入口进入 `模型回调日志`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-022 模型质量看板

- 系统：平台后台
- 路由：`admin/admin-model-quality-dashboard.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-model-quality-dashboard.html` |
| 业务域 | 平台模型运营 |
| 阶段 | P1 |
| 主要用户 | 模型运营、技术运维 |

### 页面目标

查看模型成功率、平均耗时、失败原因、成本和用户评分。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `模型质量看板` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看模型能力和任务状态
  - 操作：提交/取消/重试任务
  - 操作：比较并选择生成结果
  - 操作：切换时间范围和统计维度
  - 字段：任务/模型 ID
  - 字段：能力类型
  - 字段：模型版本
  - 字段：输入摘要
  - 字段：进度/状态
  - 字段：预估与实际成本
  - 字段：失败原因
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `GenerationTask`
  - `ModelProvider`
  - `ModelDefinition`
  - `ModelCallRecord`
  - `GeneratedAsset`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.model.view`
  - `admin.model.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：读取任务、模型能力与余额；不得用缓存余额提交付费任务。
  - 处理中：queued/running/retrying 显示服务端进度、排队位置和取消能力。
  - 异常态：failed/timeout/cancelled 必须显示归一化原因、扣费结果和可执行恢复动作。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/models`
  - `POST /api/v1/admin/models/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 模型运营、技术运维 能从规定入口进入 `模型质量看板`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-026 订单管理

- 系统：平台后台
- 路由：`admin/admin-orders.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-orders.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

查询充值、订阅、算力包和企业订单，处理支付状态、退款与关联发票。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `订单管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `订单管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-027 套餐详情

- 系统：平台后台
- 路由：`admin/admin-plan-detail.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-plan-detail.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

编辑套餐权益、席位、算力、导出、模型权限和企业服务。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `套餐详情` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 操作：查看对象完整详情与关联记录
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `套餐详情`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-028 套餐管理

- 系统：平台后台
- 路由：`admin/admin-plans.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-plans.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

配置个人、团队、企业套餐、算力包、权益和价格。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `套餐管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `套餐管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-033 财务对账

- 系统：平台后台
- 路由：`admin/admin-reconciliation.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-reconciliation.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

对账订单、支付、算力扣费、退款、发票和商单结算。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `财务对账` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `财务对账`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-034 退款管理

- 系统：平台后台
- 路由：`admin/admin-refund.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-refund.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

处理充值退款、任务失败补偿、商单退款和异常扣费。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `退款管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `退款管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-035 收入看板

- 系统：平台后台
- 路由：`admin/admin-revenue-dashboard.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-revenue-dashboard.html` |
| 业务域 | 平台财务与权益 |
| 阶段 | P1 |
| 主要用户 | 平台财务、超级管理员 |

### 页面目标

查看充值、订阅、商单、退款、ARPU 和收入趋势。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `收入看板` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看余额/成本/流水
  - 操作：筛选并导出账务数据
  - 操作：发起有审计的财务操作
  - 操作：切换时间范围和统计维度
  - 字段：账户/流水 ID
  - 字段：业务单据号
  - 字段：金额/算力最小单位
  - 字段：冻结/结算状态
  - 字段：关联任务
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `CreditAccount`
  - `CreditHold`
  - `CreditTransaction`
  - `Order`
  - `Invoice`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.finance.view`
  - `admin.finance.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：余额、冻结额和流水使用同一结算快照。
  - 空状态：无流水时展示计费说明，不伪造示例金额。
  - 异常态：对账不平、重复回调或退款处理中显示工单/人工复核编号。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/finance`
  - `POST /api/v1/admin/finance/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台财务、超级管理员 能从规定入口进入 `收入看板`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
## 6. 本模块不单独判定完成的事项

- 只有静态页面、原型或 mock 数据；
- 只有接口或数据库，没有可用交互和错误恢复；
- 只有单页测试，没有前置依赖与下游消费验证；
- 缺少实现、自动化测试、运行证据或适用发布门禁。
