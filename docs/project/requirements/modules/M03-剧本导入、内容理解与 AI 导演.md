# M03 剧本导入、内容理解与 AI 导演需求设计

- 范围：剧本与导演
- 页面覆盖：9 项（P0=8，P1=1，P2=0）
- 完成度：0/9 已验证
- 当前状态：开发中
- 联合验收包：A2 内容理解与导演闭环

- 当前实现证据：EV-M03-PERSISTENCE-001、EV-M03-RUNTIME-002、EV-M03-FRONTEND-AUTH-003
- 状态说明：已具备 SQLAlchemy 持久化、可信会话与项目归属校验、HTTP 路由及主应用挂载；剧本导入、原文管理、导演档案、映射与版本比较页面现使用真实服务端客户端并携带当前登录令牌。配置或数据库不可用时失败关闭。AI 导演模型调用、用户验收和 A2 联合验收仍未完成，模块保持开发中。

## 1. 模块目标与边界

本模块统一承载「剧本导入、内容理解与 AI 导演」相关需求。模块内页面、接口、数据、权限、异常与验收必须作为同一能力集合维护，不允许只完成可见页面而遗漏服务端规则或失败路径。

- 前置依赖：M01、M02、模型与任务能力
- 下游能力：主体资产提取、分镜规划
- 页面只做范围映射；模块完成必须通过联合验收，不以单页可打开为完成。

## 2. 功能范围与完成状态

| 需求ID | 功能/页面 | 阶段 | 状态 | 实现与验收证据 | 已知缺口 |
|---|---|---|---|---|---|
| CR-003 | AI导演 | P0 | 开发中 | EV-M03-FRONTEND-AUTH-003 | 导演档案可保存；真实 AI 导演 Provider 与 A2 联合验收未完成 |
| CR-027 | 角色场景资产 | P0 | 未开始 | — | — |
| CR-089 | 内容体检 | P0 | 未开始 | — | — |
| CR-090 | 剧本导入 | P0 | 开发中 | EV-M03-FRONTEND-AUTH-003 | 仅真实文本导入；多格式解析与 A2 联合验收未完成 |
| CR-091 | 剧本解析 | P0 | 未开始 | — | — |
| CR-092 | 原文管理 | P0 | 开发中 | EV-M03-FRONTEND-AUTH-003 | 真实版本读取与保存；真实部署和 A2 联合验收未完成 |
| CR-093 | AI 改写工作台 | P0 | 开发中 | EV-M03-FRONTEND-AUTH-003 | 保存导演档案；改写 Provider 不存在时明确不可用 |
| CR-094 | 原文映射 | P0 | 开发中 | EV-M03-FRONTEND-AUTH-003 | 映射读取依赖解析结果；真实解析与 A2 联合验收未完成 |
| CR-095 | 剧本版本对比 | P1 | 开发中 | EV-M03-FRONTEND-AUTH-003 | 真实版本比较客户端已接线；真实部署和 A2 联合验收未完成 |

## 3. 数据、权限、接口与测试覆盖

| 需求ID | 主要对象 | 权限 | API | 测试 |
|---|---|---|---|---|
| CR-003 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-003-01..06 |
| CR-027 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-027-01..06 |
| CR-089 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-089-01..06 |
| CR-090 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-090-01..06 |
| CR-091 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-091-01..06 |
| CR-092 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-092-01..06 |
| CR-093 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-093-01..06 |
| CR-094 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-094-01..06 |
| CR-095 | Script<br>ScriptVersion<br>SourceMapping<br>DirectorProfile | script.view<br>script.manage | GET /api/v1/projects/{projectId}/scripts<br>POST /api/v1/projects/{projectId}/scripts/actions | TC-CR-095-01..06 |

## 4. 联合验收要求

本模块并入「A2 内容理解与导演闭环」验收，不单独以页面数量结项。验收至少覆盖：

1. 前置依赖真实可用，权限和租户上下文贯穿全流程；
2. 模块内数据对象、状态机、API 和页面展示一致；
3. 正常、空、失败、冲突、取消、超时、重试和恢复路径按适用范围通过；
4. 模块产生的任务、费用、审计、合规和版本证据可以追踪；
5. 下游模块能够消费本模块产物，刷新或重复请求不产生重复副作用。

## 5. 详细功能需求

### CR-003 AI导演

- 系统：创作者
- 路由：`creator/ai-director.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/ai-director.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

基于冻结剧本版本生成并确认受众、节奏、镜头语言、风格和制作约束，作为资产与分镜的上游设定。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `AI导演` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `AI导演`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-027 角色场景资产

- 系统：创作者
- 路由：`creator/director-assets.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/director-assets.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

管理项目角色、场景、道具、服装和声音主体，确认主版本及其镜头引用范围。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `角色场景资产` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `角色场景资产`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-089 内容体检

- 系统：创作者
- 路由：`creator/script-checkup.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/script-checkup.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

在正式解析前检查章节、人物、场景、敏感项、字数、预计剧集和时长，并允许人工纠正。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `内容体检` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `内容体检`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-090 剧本导入

- 系统：创作者
- 路由：`creator/script-import.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/script-import.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

支持小说、剧本文档、Markdown、TXT、DOCX、分章节导入和清洗。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `剧本导入` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 操作：上传文件、映射字段并执行导入预检
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
  - 字段：文件名/类型
  - 字段：字段映射
  - 字段：校验错误
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `剧本导入`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-091 剧本解析

- 系统：创作者
- 路由：`creator/script-lab.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/script-lab.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

把导入原文解析为结构化剧本、人物关系、场景、情节点和分集建议，保留原文映射。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `剧本解析` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `剧本解析`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-092 原文管理

- 系统：创作者
- 路由：`creator/script-original.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/script-original.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

保存小说 / 剧本原文、章节结构、原文映射和可追溯引用。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `原文管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `原文管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-093 AI 改写工作台

- 系统：创作者
- 路由：`creator/script-rewrite.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/script-rewrite.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

短剧化改编、台词润色、旁白压缩、冲突增强和版本生成。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `AI 改写工作台` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `AI 改写工作台`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-094 原文映射

- 系统：创作者
- 路由：`creator/script-source-mapping.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/script-source-mapping.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P0 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

将剧本、分镜、对白、旁白映射回原小说章节，保证可追溯。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `原文映射` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `原文映射`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-095 剧本版本对比

- 系统：创作者
- 路由：`creator/script-version-compare.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/script-version-compare.html` |
| 业务域 | 剧本与导演 |
| 阶段 | P1 |
| 主要用户 | 编剧、导演、项目负责人 |

### 页面目标

对比原文、改写版、审稿版和冻结版差异，可局部回滚。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `剧本版本对比` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：读取原文与当前版本
  - 操作：编辑或生成内容
  - 操作：对比并冻结版本
  - 操作：并排查看差异并选择保留版本
  - 字段：剧本 ID
  - 字段：来源章节
  - 字段：当前版本
  - 字段：人物/场景
  - 字段：节奏/风格
  - 字段：锁定状态
  - 字段：基准版本
  - 字段：候选版本
  - 字段：差异项
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Script`
  - `ScriptVersion`
  - `SourceMapping`
  - `DirectorProfile`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `script.view`
  - `script.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：恢复当前工作区、项目、剧集及对象版本。
  - 编辑态：自动保存显示保存中/已保存/冲突，冲突不得静默覆盖。
  - 异常态：权限变化、对象删除、版本冲突和前置条件未满足均提供恢复路径。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/projects/{projectId}/scripts`
  - `POST /api/v1/projects/{projectId}/scripts/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 编剧、导演、项目负责人 能从规定入口进入 `剧本版本对比`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
## 6. 本模块不单独判定完成的事项

- 只有静态页面、原型或 mock 数据；
- 只有接口或数据库，没有可用交互和错误恢复；
- 只有单页测试，没有前置依赖与下游消费验证；
- 缺少实现、自动化测试、运行证据或适用发布门禁。
