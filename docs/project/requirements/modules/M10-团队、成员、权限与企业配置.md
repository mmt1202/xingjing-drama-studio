# M10 团队、成员、权限与企业配置需求设计

- 范围：团队成员与权限、团队总览、团队审计、企业配置
- 页面覆盖：11 项（P0=0，P1=11，P2=0）
- 完成度：0/11 已验证
- 当前状态：开发中
- 联合验收包：B1 团队治理闭环

- 当前实现证据：EV-M10-M14-IMPL-001、EV-M10-M14-TEST-001
- 状态说明：页面组件、typed API 与主路由已实现；身份/RBAC/审计联合验收未完成。

## 1. 模块目标与边界

本模块统一承载「团队、成员、权限与企业配置」相关需求。模块内页面、接口、数据、权限、异常与验收必须作为同一能力集合维护，不允许只完成可见页面而遗漏服务端规则或失败路径。

- 前置依赖：M01、M02、审计与 RBAC
- 下游能力：规模化协作、团队计费和客户交付
- 页面只做范围映射；模块完成必须通过联合验收，不以单页可打开为完成。

## 2. 功能范围与完成状态

| 需求ID | 功能/页面 | 阶段 | 状态 | 实现与验收证据 | 已知缺口 |
|---|---|---|---|---|---|
| TM-006 | 企业认证 | P1 | 未开始 | — | — |
| TM-007 | 企业配置 | P1 | 未开始 | — | — |
| TM-008 | 成员邀请记录 | P1 | 未开始 | — | — |
| TM-009 | 成员人效看板 | P1 | 未开始 | — | — |
| TM-010 | 成员管理 | P1 | 未开始 | — | — |
| TM-011 | 团队操作日志 | P1 | 未开始 | — | — |
| TM-012 | 团队总览 | P1 | 未开始 | — | — |
| TM-013 | 团队权限矩阵 | P1 | 未开始 | — | — |
| TM-016 | 角色权限 | P1 | 未开始 | — | — |
| TM-017 | 席位管理 | P1 | 未开始 | — | — |
| TM-018 | 团队空间总览 | P1 | 未开始 | — | — |

## 3. 数据、权限、接口与测试覆盖

| 需求ID | 主要对象 | 权限 | API | 测试 |
|---|---|---|---|---|
| TM-006 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.enterprise.view<br>workspace.enterprise.manage | GET /api/v1/workspaces/{workspaceId}/enterprise-profile<br>PUT /api/v1/workspaces/{workspaceId}/enterprise-profile | TC-TM-006-01..06 |
| TM-007 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.enterprise.view<br>workspace.enterprise.manage | GET /api/v1/workspaces/{workspaceId}/enterprise-profile<br>PUT /api/v1/workspaces/{workspaceId}/enterprise-profile | TC-TM-007-01..06 |
| TM-008 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.member.view<br>workspace.member.manage | GET /api/v1/workspaces/{workspaceId}/members<br>PUT /api/v1/workspaces/{workspaceId}/roles/{roleId} | TC-TM-008-01..06 |
| TM-009 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.member.view<br>workspace.member.manage | GET /api/v1/workspaces/{workspaceId}/members<br>PUT /api/v1/workspaces/{workspaceId}/roles/{roleId} | TC-TM-009-01..06 |
| TM-010 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.member.view<br>workspace.member.manage | GET /api/v1/workspaces/{workspaceId}/members<br>PUT /api/v1/workspaces/{workspaceId}/roles/{roleId} | TC-TM-010-01..06 |
| TM-011 | AdminStaff<br>AdminRole<br>AuditLog<br>ApprovalRequest | workspace.audit.view<br>workspace.audit.manage | GET /api/v1/workspaces/{workspaceId}/overview | TC-TM-011-01..06 |
| TM-012 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.view<br>workspace.manage | GET /api/v1/workspaces/{workspaceId}/overview | TC-TM-012-01..06 |
| TM-013 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.member.view<br>workspace.member.manage | GET /api/v1/workspaces/{workspaceId}/members<br>PUT /api/v1/workspaces/{workspaceId}/roles/{roleId} | TC-TM-013-01..06 |
| TM-016 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.member.view<br>workspace.member.manage | GET /api/v1/workspaces/{workspaceId}/members<br>PUT /api/v1/workspaces/{workspaceId}/roles/{roleId} | TC-TM-016-01..06 |
| TM-017 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.member.view<br>workspace.member.manage | GET /api/v1/workspaces/{workspaceId}/members<br>PUT /api/v1/workspaces/{workspaceId}/roles/{roleId} | TC-TM-017-01..06 |
| TM-018 | Workspace<br>WorkspaceMember<br>Role<br>Permission<br>AuditLog | workspace.view<br>workspace.manage | GET /api/v1/workspaces/{workspaceId}/overview | TC-TM-018-01..06 |

## 4. 联合验收要求

本模块并入「B1 团队治理闭环」验收，不单独以页面数量结项。验收至少覆盖：

1. 前置依赖真实可用，权限和租户上下文贯穿全流程；
2. 模块内数据对象、状态机、API 和页面展示一致；
3. 正常、空、失败、冲突、取消、超时、重试和恢复路径按适用范围通过；
4. 模块产生的任务、费用、审计、合规和版本证据可以追踪；
5. 下游模块能够消费本模块产物，刷新或重复请求不产生重复副作用。

## 5. 详细功能需求

### TM-006 企业认证

- 系统：团队
- 路由：`team/team-enterprise-auth.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-enterprise-auth.html` |
| 业务域 | 企业配置 |
| 阶段 | P1 |
| 主要用户 | 企业管理员、团队所有者 |

### 页面目标

企业实名、合同主体、发票信息、安全配置和私有化预留。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `企业认证` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.enterprise.view`
  - `workspace.enterprise.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/enterprise-profile`
  - `PUT /api/v1/workspaces/{workspaceId}/enterprise-profile`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 企业管理员、团队所有者 能从规定入口进入 `企业认证`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-007 企业配置

- 系统：团队
- 路由：`team/team-enterprise.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-enterprise.html` |
| 业务域 | 企业配置 |
| 阶段 | P1 |
| 主要用户 | 企业管理员、团队所有者 |

### 页面目标

管理企业主体、认证、发票、安全策略、数据保留和专有部署相关配置。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `企业配置` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.enterprise.view`
  - `workspace.enterprise.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/enterprise-profile`
  - `PUT /api/v1/workspaces/{workspaceId}/enterprise-profile`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 企业管理员、团队所有者 能从规定入口进入 `企业配置`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-008 成员邀请记录

- 系统：团队
- 路由：`team/team-invite-records.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-invite-records.html` |
| 业务域 | 团队成员与权限 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

查看邀请链接、邀请人、有效期、接受状态和撤回记录。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `成员邀请记录` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.member.view`
  - `workspace.member.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：以服务端会话和权限为准。
  - 禁用态：无权限操作不渲染或禁用并说明原因，接口仍必须拒绝。
  - 异常态：登录风险、验证失败和会话失效不得泄露账号是否存在。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/members`
  - `PUT /api/v1/workspaces/{workspaceId}/roles/{roleId}`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `成员邀请记录`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-009 成员人效看板

- 系统：团队
- 路由：`team/team-member-performance.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-member-performance.html` |
| 业务域 | 团队成员与权限 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

统计成员任务量、通过率、返工率、成本和审片效率。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `成员人效看板` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.member.view`
  - `workspace.member.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：以服务端会话和权限为准。
  - 禁用态：无权限操作不渲染或禁用并说明原因，接口仍必须拒绝。
  - 异常态：登录风险、验证失败和会话失效不得泄露账号是否存在。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/members`
  - `PUT /api/v1/workspaces/{workspaceId}/roles/{roleId}`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `成员人效看板`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-010 成员管理

- 系统：团队
- 路由：`team/team-members.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-members.html` |
| 业务域 | 团队成员与权限 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

邀请、启用、停用和移除成员，分配角色、席位并查看其项目范围。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `成员管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.member.view`
  - `workspace.member.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：以服务端会话和权限为准。
  - 禁用态：无权限操作不渲染或禁用并说明原因，接口仍必须拒绝。
  - 异常态：登录风险、验证失败和会话失效不得泄露账号是否存在。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/members`
  - `PUT /api/v1/workspaces/{workspaceId}/roles/{roleId}`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `成员管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-011 团队操作日志

- 系统：团队
- 路由：`team/team-operation-logs.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-operation-logs.html` |
| 业务域 | 团队审计 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

记录成员、权限、席位、账单、项目访问和敏感操作。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `团队操作日志` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：筛选安全或审计记录
  - 操作：查看变更前后证据
  - 操作：审批或处置高风险动作
  - 操作：按请求 ID、对象和操作者检索日志
  - 字段：日志/审批 ID
  - 字段：操作者
  - 字段：对象
  - 字段：动作
  - 字段：变更摘要
  - 字段：IP/设备
  - 字段：结果
  - 字段：发生时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `AdminStaff`
  - `AdminRole`
  - `AuditLog`
  - `ApprovalRequest`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.audit.view`
  - `workspace.audit.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/overview`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `团队操作日志`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-012 团队总览

- 系统：团队
- 路由：`team/team-overview.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-overview.html` |
| 业务域 | 团队总览 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

聚合团队成员、项目进度、算力、成本、审片和异常，提供治理模块入口。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `团队总览` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 操作：切换时间范围和统计维度
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.view`
  - `workspace.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/overview`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `团队总览`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-013 团队权限矩阵

- 系统：团队
- 路由：`team/team-permission-matrix.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-permission-matrix.html` |
| 业务域 | 团队成员与权限 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

按角色配置项目、剧集、资产、生成、导出、账单和审片权限。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `团队权限矩阵` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 操作：预览权限影响并保存矩阵版本
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.member.view`
  - `workspace.member.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：以服务端会话和权限为准。
  - 禁用态：无权限操作不渲染或禁用并说明原因，接口仍必须拒绝。
  - 异常态：登录风险、验证失败和会话失效不得泄露账号是否存在。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/members`
  - `PUT /api/v1/workspaces/{workspaceId}/roles/{roleId}`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `团队权限矩阵`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-016 角色权限

- 系统：团队
- 路由：`team/team-roles.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-roles.html` |
| 业务域 | 团队成员与权限 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

创建和维护团队角色，配置菜单、动作和数据范围并预览权限影响。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `角色权限` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 操作：预览权限影响并保存矩阵版本
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.member.view`
  - `workspace.member.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：以服务端会话和权限为准。
  - 禁用态：无权限操作不渲染或禁用并说明原因，接口仍必须拒绝。
  - 异常态：登录风险、验证失败和会话失效不得泄露账号是否存在。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/members`
  - `PUT /api/v1/workspaces/{workspaceId}/roles/{roleId}`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `角色权限`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-017 席位管理

- 系统：团队
- 路由：`team/team-seats.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-seats.html` |
| 业务域 | 团队成员与权限 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

查看套餐席位总量、占用、到期与成员分配，处理回收和超额问题。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `席位管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.member.view`
  - `workspace.member.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：以服务端会话和权限为准。
  - 禁用态：无权限操作不渲染或禁用并说明原因，接口仍必须拒绝。
  - 异常态：登录风险、验证失败和会话失效不得泄露账号是否存在。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/members`
  - `PUT /api/v1/workspaces/{workspaceId}/roles/{roleId}`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `席位管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### TM-018 团队空间总览

- 系统：团队
- 路由：`team/team-space.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 团队/企业后台 |
| 原型路由 | `team/team-space.html` |
| 业务域 | 团队总览 |
| 阶段 | P1 |
| 主要用户 | 团队所有者、团队管理员 |

### 页面目标

作为团队后台入口展示团队状态、管理员操作和近期治理事件。

### 入口、前置与退出

- 进入页面前必须解析团队工作区及管理员数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `团队空间总览` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看工作区范围
  - 操作：维护成员或配置
  - 操作：查看变更记录
  - 字段：工作区 ID
  - 字段：成员/角色
  - 字段：数据范围
  - 字段：席位/套餐
  - 字段：状态
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Workspace`
  - `WorkspaceMember`
  - `Role`
  - `Permission`
  - `AuditLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `workspace.view`
  - `workspace.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/workspaces/{workspaceId}/overview`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 团队所有者、团队管理员 能从规定入口进入 `团队空间总览`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
## 6. 本模块不单独判定完成的事项

- 只有静态页面、原型或 mock 数据；
- 只有接口或数据库，没有可用交互和错误恢复；
- 只有单页测试，没有前置依赖与下游消费验证；
- 缺少实现、自动化测试、运行证据或适用发布门禁。
