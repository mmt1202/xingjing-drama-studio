# M18 平台运维、客服、消息与运营配置需求设计

- 范围：平台运维、平台客服、平台消息、平台运营配置、平台设置
- 页面覆盖：18 项（P0=1，P1=17，P2=0）
- 完成度：0/18 已验证
- 当前状态：未开始
- 联合验收包：D4 平台运营保障闭环

## 1. 模块目标与边界

本模块统一承载「平台运维、客服、消息与运营配置」相关需求。模块内页面、接口、数据、权限、异常与验收必须作为同一能力集合维护，不允许只完成可见页面而遗漏服务端规则或失败路径。

- 前置依赖：平台共享地基、监控、通知与审计
- 下游能力：稳定运营、客服处置、消息触达和策略配置
- 页面只做范围映射；模块完成必须通过联合验收，不以单页可打开为完成。

## 2. 功能范围与完成状态

| 需求ID | 功能/页面 | 阶段 | 状态 | 实现与验收证据 | 已知缺口 |
|---|---|---|---|---|---|
| AD-040 | 服务状态监控 | P0 | 未开始 | — | — |
| AD-001 | 告警规则 | P1 | 未开始 | — | — |
| AD-014 | 异常日志 | P1 | 未开始 | — | — |
| AD-016 | 增长数据看板 | P1 | 未开始 | — | — |
| AD-024 | 通知中心 | P1 | 未开始 | — | — |
| AD-025 | 运营配置 | P1 | 未开始 | — | — |
| AD-029 | 平台档案配置 | P1 | 未开始 | — | — |
| AD-031 | 发布规则管理 | P1 | 未开始 | — | — |
| AD-032 | 队列监控 | P1 | 未开始 | — | — |
| AD-042 | 存储监控 | P1 | 未开始 | — | — |
| AD-043 | 客服支持 | P1 | 未开始 | — | — |
| AD-046 | 任务通知规则 | P1 | 未开始 | — | — |
| CR-022 | 封面生成 | P1 | 未开始 | — | — |
| CR-023 | 封面标题 A/B | P1 | 未开始 | — | — |
| CR-046 | 创作首页 | P1 | 未开始 | — | — |
| CR-087 | 风险申诉 | P1 | 未开始 | — | — |
| CR-096 | 设置 | P1 | 未开始 | — | — |
| CR-115 | 版本记录 | P1 | 未开始 | — | — |

## 3. 数据、权限、接口与测试覆盖

| 需求ID | 主要对象 | 权限 | API | 测试 |
|---|---|---|---|---|
| AD-040 | ServiceHealth<br>QueueMetric<br>AlertRule<br>Incident | admin.ops.view<br>admin.ops.manage | GET /api/v1/admin/operations<br>POST /api/v1/admin/operations/actions | TC-AD-040-01..06 |
| AD-001 | ServiceHealth<br>QueueMetric<br>AlertRule<br>Incident | admin.ops.view<br>admin.ops.manage | GET /api/v1/admin/operations<br>POST /api/v1/admin/operations/actions | TC-AD-001-01..06 |
| AD-014 | ServiceHealth<br>QueueMetric<br>AlertRule<br>Incident | admin.ops.view<br>admin.ops.manage | GET /api/v1/admin/operations<br>POST /api/v1/admin/operations/actions | TC-AD-014-01..06 |
| AD-016 | ExportTask<br>PublishPackage<br>PlatformRule<br>ComplianceRecord | admin.business.view<br>admin.business.manage | GET /api/v1/admin/operations-config<br>POST /api/v1/admin/operations-config/actions | TC-AD-016-01..06 |
| AD-024 | Notification<br>MessageTemplate<br>DeliveryLog | admin.notification.view<br>admin.notification.manage | GET /api/v1/admin/notifications<br>POST /api/v1/admin/notifications/actions | TC-AD-024-01..06 |
| AD-025 | ExportTask<br>PublishPackage<br>PlatformRule<br>ComplianceRecord | admin.business.view<br>admin.business.manage | GET /api/v1/admin/operations-config<br>POST /api/v1/admin/operations-config/actions | TC-AD-025-01..06 |
| AD-029 | ExportTask<br>PublishPackage<br>PlatformRule<br>ComplianceRecord | admin.business.view<br>admin.business.manage | GET /api/v1/admin/operations-config<br>POST /api/v1/admin/operations-config/actions | TC-AD-029-01..06 |
| AD-031 | ExportTask<br>PublishPackage<br>PlatformRule<br>ComplianceRecord | admin.business.view<br>admin.business.manage | GET /api/v1/admin/operations-config<br>POST /api/v1/admin/operations-config/actions | TC-AD-031-01..06 |
| AD-032 | ServiceHealth<br>QueueMetric<br>AlertRule<br>Incident | admin.ops.view<br>admin.ops.manage | GET /api/v1/admin/operations<br>POST /api/v1/admin/operations/actions | TC-AD-032-01..06 |
| AD-042 | ServiceHealth<br>QueueMetric<br>AlertRule<br>Incident | admin.ops.view<br>admin.ops.manage | GET /api/v1/admin/operations<br>POST /api/v1/admin/operations/actions | TC-AD-042-01..06 |
| AD-043 | SupportTicket<br>TicketMessage<br>CompensationRecord | admin.support.view<br>admin.support.manage | GET /api/v1/admin/support-tickets<br>POST /api/v1/admin/support-tickets/actions | TC-AD-043-01..06 |
| AD-046 | Notification<br>MessageTemplate<br>DeliveryLog | admin.notification.view<br>admin.notification.manage | GET /api/v1/admin/notifications<br>POST /api/v1/admin/notifications/actions | TC-AD-046-01..06 |
| CR-022 | UserPreference<br>FeatureFlag | settings.view<br>settings.manage | GET /api/v1/account/preferences<br>PUT /api/v1/account/preferences | TC-CR-022-01..06 |
| CR-023 | UserPreference<br>FeatureFlag | settings.view<br>settings.manage | GET /api/v1/account/preferences<br>PUT /api/v1/account/preferences | TC-CR-023-01..06 |
| CR-046 | UserPreference<br>FeatureFlag | settings.view<br>settings.manage | GET /api/v1/account/preferences<br>PUT /api/v1/account/preferences | TC-CR-046-01..06 |
| CR-087 | UserPreference<br>FeatureFlag | settings.view<br>settings.manage | GET /api/v1/account/preferences<br>PUT /api/v1/account/preferences | TC-CR-087-01..06 |
| CR-096 | UserPreference<br>FeatureFlag | settings.view<br>settings.manage | GET /api/v1/account/preferences<br>PUT /api/v1/account/preferences | TC-CR-096-01..06 |
| CR-115 | UserPreference<br>FeatureFlag | settings.view<br>settings.manage | GET /api/v1/account/preferences<br>PUT /api/v1/account/preferences | TC-CR-115-01..06 |

## 4. 联合验收要求

本模块并入「D4 平台运营保障闭环」验收，不单独以页面数量结项。验收至少覆盖：

1. 前置依赖真实可用，权限和租户上下文贯穿全流程；
2. 模块内数据对象、状态机、API 和页面展示一致；
3. 正常、空、失败、冲突、取消、超时、重试和恢复路径按适用范围通过；
4. 模块产生的任务、费用、审计、合规和版本证据可以追踪；
5. 下游模块能够消费本模块产物，刷新或重复请求不产生重复副作用。

## 5. 详细功能需求

### AD-040 服务状态监控

- 系统：平台后台
- 路由：`admin/admin-service-status.html`
- 阶段：P0
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-service-status.html` |
| 业务域 | 平台运维 |
| 阶段 | P0 |
| 主要用户 | 技术运维、超级管理员 |

### 页面目标

查看网关、Java 服务、AI 服务、Worker、存储、Redis、MQ 状态。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `服务状态监控` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看实时指标和异常
  - 操作：调整规则或执行受控操作
  - 操作：确认恢复并关闭事件
  - 字段：服务/队列/事件 ID
  - 字段：环境
  - 字段：指标值
  - 字段：阈值
  - 字段：严重程度
  - 字段：负责人
  - 字段：状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ServiceHealth`
  - `QueueMetric`
  - `AlertRule`
  - `Incident`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.ops.view`
  - `admin.ops.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations`
  - `POST /api/v1/admin/operations/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 技术运维、超级管理员 能从规定入口进入 `服务状态监控`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-001 告警规则

- 系统：平台后台
- 路由：`admin/admin-alert-rules.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-alert-rules.html` |
| 业务域 | 平台运维 |
| 阶段 | P1 |
| 主要用户 | 技术运维、超级管理员 |

### 页面目标

配置服务异常、队列积压、模型失败率、存储不足和费用异常告警。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `告警规则` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看实时指标和异常
  - 操作：调整规则或执行受控操作
  - 操作：确认恢复并关闭事件
  - 字段：服务/队列/事件 ID
  - 字段：环境
  - 字段：指标值
  - 字段：阈值
  - 字段：严重程度
  - 字段：负责人
  - 字段：状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ServiceHealth`
  - `QueueMetric`
  - `AlertRule`
  - `Incident`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.ops.view`
  - `admin.ops.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations`
  - `POST /api/v1/admin/operations/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 技术运维、超级管理员 能从规定入口进入 `告警规则`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-014 异常日志

- 系统：平台后台
- 路由：`admin/admin-error-log.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-error-log.html` |
| 业务域 | 平台运维 |
| 阶段 | P1 |
| 主要用户 | 技术运维、超级管理员 |

### 页面目标

查询接口异常、模型异常、回调异常、任务异常和导出异常。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `异常日志` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看实时指标和异常
  - 操作：调整规则或执行受控操作
  - 操作：确认恢复并关闭事件
  - 操作：按请求 ID、对象和操作者检索日志
  - 字段：服务/队列/事件 ID
  - 字段：环境
  - 字段：指标值
  - 字段：阈值
  - 字段：严重程度
  - 字段：负责人
  - 字段：状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ServiceHealth`
  - `QueueMetric`
  - `AlertRule`
  - `Incident`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.ops.view`
  - `admin.ops.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations`
  - `POST /api/v1/admin/operations/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 技术运维、超级管理员 能从规定入口进入 `异常日志`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-016 增长数据看板

- 系统：平台后台
- 路由：`admin/admin-growth-dashboard.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-growth-dashboard.html` |
| 业务域 | 平台运营配置 |
| 阶段 | P1 |
| 主要用户 | 平台运营 |

### 页面目标

查看注册、活跃、留存、转化、项目创建和发布数据。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `增长数据看板` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：配置目标格式或平台规则
  - 操作：执行导出前校验
  - 操作：提交任务并下载结果
  - 操作：切换时间范围和统计维度
  - 字段：导出/发布包 ID
  - 字段：目标平台
  - 字段：格式/比例
  - 字段：项目版本
  - 字段：合规状态
  - 字段：任务状态
  - 字段：下载有效期
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ExportTask`
  - `PublishPackage`
  - `PlatformRule`
  - `ComplianceRecord`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations-config`
  - `POST /api/v1/admin/operations-config/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营 能从规定入口进入 `增长数据看板`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-024 通知中心

- 系统：平台后台
- 路由：`admin/admin-notifications.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-notifications.html` |
| 业务域 | 平台消息 |
| 阶段 | P1 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

管理系统公告、任务通知、扣费通知、审核通知和客服通知。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `通知中心` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：筛选消息和发送记录
  - 操作：编辑模板或规则
  - 操作：预览、测试并发布
  - 字段：模板/通知 ID
  - 字段：渠道
  - 字段：受众
  - 字段：变量
  - 字段：发送策略
  - 字段：送达状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Notification`
  - `MessageTemplate`
  - `DeliveryLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.notification.view`
  - `admin.notification.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/notifications`
  - `POST /api/v1/admin/notifications/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `通知中心`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-025 运营配置

- 系统：平台后台
- 路由：`admin/admin-ops.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-ops.html` |
| 业务域 | 平台运营配置 |
| 阶段 | P1 |
| 主要用户 | 平台运营 |

### 页面目标

管理活动、推荐、消息、平台规则、功能开关和其他版本化运营配置。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `运营配置` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：配置目标格式或平台规则
  - 操作：执行导出前校验
  - 操作：提交任务并下载结果
  - 字段：导出/发布包 ID
  - 字段：目标平台
  - 字段：格式/比例
  - 字段：项目版本
  - 字段：合规状态
  - 字段：任务状态
  - 字段：下载有效期
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ExportTask`
  - `PublishPackage`
  - `PlatformRule`
  - `ComplianceRecord`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations-config`
  - `POST /api/v1/admin/operations-config/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营 能从规定入口进入 `运营配置`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-029 平台档案配置

- 系统：平台后台
- 路由：`admin/admin-platform-profile.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-platform-profile.html` |
| 业务域 | 平台运营配置 |
| 阶段 | P1 |
| 主要用户 | 平台运营 |

### 页面目标

维护抖音、快手、视频号、小红书、红果、TikTok 等平台规格。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `平台档案配置` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：配置目标格式或平台规则
  - 操作：执行导出前校验
  - 操作：提交任务并下载结果
  - 字段：导出/发布包 ID
  - 字段：目标平台
  - 字段：格式/比例
  - 字段：项目版本
  - 字段：合规状态
  - 字段：任务状态
  - 字段：下载有效期
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ExportTask`
  - `PublishPackage`
  - `PlatformRule`
  - `ComplianceRecord`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations-config`
  - `POST /api/v1/admin/operations-config/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营 能从规定入口进入 `平台档案配置`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-031 发布规则管理

- 系统：平台后台
- 路由：`admin/admin-publish-rules.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-publish-rules.html` |
| 业务域 | 平台运营配置 |
| 阶段 | P1 |
| 主要用户 | 平台运营 |

### 页面目标

配置各平台封面、标题、标签、比例、AIGC 标识和素材包规则。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `发布规则管理` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：配置目标格式或平台规则
  - 操作：执行导出前校验
  - 操作：提交任务并下载结果
  - 字段：导出/发布包 ID
  - 字段：目标平台
  - 字段：格式/比例
  - 字段：项目版本
  - 字段：合规状态
  - 字段：任务状态
  - 字段：下载有效期
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ExportTask`
  - `PublishPackage`
  - `PlatformRule`
  - `ComplianceRecord`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.business.view`
  - `admin.business.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations-config`
  - `POST /api/v1/admin/operations-config/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营 能从规定入口进入 `发布规则管理`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-032 队列监控

- 系统：平台后台
- 路由：`admin/admin-queue-monitor.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-queue-monitor.html` |
| 业务域 | 平台运维 |
| 阶段 | P1 |
| 主要用户 | 技术运维、超级管理员 |

### 页面目标

监控生成队列、导出队列、死信队列、重试队列和积压情况。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `队列监控` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看实时指标和异常
  - 操作：调整规则或执行受控操作
  - 操作：确认恢复并关闭事件
  - 字段：服务/队列/事件 ID
  - 字段：环境
  - 字段：指标值
  - 字段：阈值
  - 字段：严重程度
  - 字段：负责人
  - 字段：状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ServiceHealth`
  - `QueueMetric`
  - `AlertRule`
  - `Incident`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.ops.view`
  - `admin.ops.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations`
  - `POST /api/v1/admin/operations/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 技术运维、超级管理员 能从规定入口进入 `队列监控`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-042 存储监控

- 系统：平台后台
- 路由：`admin/admin-storage-monitor.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-storage-monitor.html` |
| 业务域 | 平台运维 |
| 阶段 | P1 |
| 主要用户 | 技术运维、超级管理员 |

### 页面目标

监控对象存储容量、热点文件、生命周期清理和上传失败。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `存储监控` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看实时指标和异常
  - 操作：调整规则或执行受控操作
  - 操作：确认恢复并关闭事件
  - 字段：服务/队列/事件 ID
  - 字段：环境
  - 字段：指标值
  - 字段：阈值
  - 字段：严重程度
  - 字段：负责人
  - 字段：状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `ServiceHealth`
  - `QueueMetric`
  - `AlertRule`
  - `Incident`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.ops.view`
  - `admin.ops.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/operations`
  - `POST /api/v1/admin/operations/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 技术运维、超级管理员 能从规定入口进入 `存储监控`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-043 客服支持

- 系统：平台后台
- 路由：`admin/admin-support.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-support.html` |
| 业务域 | 平台客服 |
| 阶段 | P1 |
| 主要用户 | 客服人员 |

### 页面目标

处理用户、生成失败、扣费、订单、审片和合规工单，关联受控补偿动作。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `客服支持` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查询关联用户和业务对象
  - 操作：回复、转派或升级工单
  - 操作：执行授权补偿并关闭工单
  - 字段：工单 ID
  - 字段：用户/团队
  - 字段：问题类型
  - 字段：关联对象
  - 字段：优先级
  - 字段：处理人
  - 字段：状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `SupportTicket`
  - `TicketMessage`
  - `CompensationRecord`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.support.view`
  - `admin.support.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/support-tickets`
  - `POST /api/v1/admin/support-tickets/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 客服人员 能从规定入口进入 `客服支持`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### AD-046 任务通知规则

- 系统：平台后台
- 路由：`admin/admin-task-notice-rules.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 平台运营后台 |
| 原型路由 | `admin/admin-task-notice-rules.html` |
| 业务域 | 平台消息 |
| 阶段 | P1 |
| 主要用户 | 平台运营、只读管理员 |

### 页面目标

配置任务失败、长时间排队、扣费异常、导出完成的通知策略。

### 入口、前置与退出

- 进入页面前必须验证后台员工独立会话、角色和数据范围。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `任务通知规则` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：筛选消息和发送记录
  - 操作：编辑模板或规则
  - 操作：预览、测试并发布
  - 字段：模板/通知 ID
  - 字段：渠道
  - 字段：受众
  - 字段：变量
  - 字段：发送策略
  - 字段：送达状态
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `Notification`
  - `MessageTemplate`
  - `DeliveryLog`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `admin.notification.view`
  - `admin.notification.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/admin/notifications`
  - `POST /api/v1/admin/notifications/actions`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 平台运营、只读管理员 能从规定入口进入 `任务通知规则`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-022 封面生成

- 系统：创作者
- 路由：`creator/cover-generate.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/cover-generate.html` |
| 业务域 | 平台设置 |
| 阶段 | P1 |
| 主要用户 | 账号所有者 |

### 页面目标

为抖音、快手、视频号、小红书生成封面、标题字区和 A/B 候选。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `封面生成` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看封面生成
  - 操作：更新封面生成 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `UserPreference`
  - `FeatureFlag`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `settings.view`
  - `settings.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/account/preferences`
  - `PUT /api/v1/account/preferences`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 账号所有者 能从规定入口进入 `封面生成`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-023 封面标题 A/B

- 系统：创作者
- 路由：`creator/cover-title-ab-test.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/cover-title-ab-test.html` |
| 业务域 | 平台设置 |
| 阶段 | P1 |
| 主要用户 | 账号所有者 |

### 页面目标

生成和评估多组封面、标题、简介、标签组合。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `封面标题 A/B` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看封面标题 A/B
  - 操作：更新封面标题 A/B 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `UserPreference`
  - `FeatureFlag`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `settings.view`
  - `settings.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/account/preferences`
  - `PUT /api/v1/account/preferences`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 账号所有者 能从规定入口进入 `封面标题 A/B`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-046 创作首页

- 系统：创作者
- 路由：`creator/home.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/home.html` |
| 业务域 | 平台设置 |
| 阶段 | P1 |
| 主要用户 | 账号所有者 |

### 页面目标

提供平台级创作入口、最近项目、快速入口、灵感与案例，不混入具体项目的生产阶段导航。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `创作首页` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看创作首页
  - 操作：更新创作首页 配置
  - 操作：保存并审计变更
  - 操作：切换时间范围和统计维度
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `UserPreference`
  - `FeatureFlag`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `settings.view`
  - `settings.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/account/preferences`
  - `PUT /api/v1/account/preferences`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 账号所有者 能从规定入口进入 `创作首页`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-087 风险申诉

- 系统：创作者
- 路由：`creator/risk-appeal.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/risk-appeal.html` |
| 业务域 | 平台设置 |
| 阶段 | P1 |
| 主要用户 | 账号所有者 |

### 页面目标

对合规阻断、版权风险、真人相似风险提交申诉和复核。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `风险申诉` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看风险申诉
  - 操作：更新风险申诉 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `UserPreference`
  - `FeatureFlag`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `settings.view`
  - `settings.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/account/preferences`
  - `PUT /api/v1/account/preferences`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 账号所有者 能从规定入口进入 `风险申诉`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-096 设置

- 系统：创作者
- 路由：`creator/settings.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/settings.html` |
| 业务域 | 平台设置 |
| 阶段 | P1 |
| 主要用户 | 账号所有者 |

### 页面目标

管理账号偏好、默认工作区、通知、生成默认值、媒体展示和安全入口。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `设置` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看设置
  - 操作：更新设置 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `UserPreference`
  - `FeatureFlag`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `settings.view`
  - `settings.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/account/preferences`
  - `PUT /api/v1/account/preferences`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 账号所有者 能从规定入口进入 `设置`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
### CR-115 版本记录

- 系统：创作者
- 路由：`creator/version-history.html`
- 阶段：P1
- 当前状态：未开始


| 属性 | 规格 |
|---|---|
| 系统 | 创作者前台 |
| 原型路由 | `creator/version-history.html` |
| 业务域 | 平台设置 |
| 阶段 | P1 |
| 主要用户 | 账号所有者 |

### 页面目标

按剧本、资产、分镜、故事板和成片查看版本链、差异、锁定、回滚和来源。

### 入口、前置与退出

- 进入页面前必须解析当前工作区；项目内页面还必须解析项目、剧集或对象上下文。
- 入口可以来自所属一级模块、对象详情、任务/通知深链或具备权限的关联页面。
- 返回列表时恢复筛选、分页和滚动；深链跳转保留工作区与对象上下文。

### 页面区域、字段与操作

- 顶部区域必须展示页面标题、当前上下文、关键状态和主操作。
- 主内容区展示与 `版本记录` 直接相关的数据，不使用与业务无关的通用占位卡片。
  - 操作：查看版本记录
  - 操作：更新版本记录 配置
  - 操作：保存并审计变更
  - 字段：对象 ID
  - 字段：名称
  - 字段：状态
  - 字段：配置值
  - 字段：更新时间
- 列表支持服务端分页、稳定排序和组合筛选；详情显示对象 ID、版本和更新时间。

### 数据对象与业务规则

  - `UserPreference`
  - `FeatureFlag`
- 所有查询按租户和权限范围过滤；跨对象引用校验同一工作区或明确授权关系。
- 写入携带 `Idempotency-Key`；更新携带对象 `version`，冲突返回 `409 VERSION_CONFLICT`。

### 权限与数据范围

  - `settings.view`
  - `settings.manage`
- 前端可见性不能替代服务端鉴权；权限变化后下一次请求立即生效。
- 所有新增、修改、提交、审批、导出、删除或高风险动作记录操作者、时间、对象、变更前后摘要、请求 ID 和结果。

### 页面状态与异常

  - 加载态：展示骨架屏并保留页面结构。
  - 空状态：说明无数据原因并提供当前用户有权限的下一步。
  - 异常态：区分无权限、无数据、服务失败和并发冲突。
- 网络重试不得重复提交写操作；恢复后重新读取服务端最终状态。

### 接口依赖

  - `GET /api/v1/account/preferences`
  - `PUT /api/v1/account/preferences`
- 通用依赖：`GET /api/v1/session/context` 获取当前用户、工作区、角色和功能开关。

### 验收标准

1. 具备权限的 账号所有者 能从规定入口进入 `版本记录`，页面上下文、标题和数据范围正确。
2. 无权限用户直接访问路由时服务端返回 403，页面不泄露对象名称、媒体地址或敏感字段。
3. 页面加载、空数据、处理中、成功、失败和并发冲突状态均有明确反馈和可执行恢复动作。
4. 所有写操作通过服务端校验、幂等和乐观锁；成功后重新读取的数据与提交结果一致。
5. 审计要求覆盖的操作可以按请求 ID、操作者和业务对象检索。
6. 页面接口失败不会丢失已保存数据，也不会产生重复任务、重复扣费或重复审批。

---
## 6. 本模块不单独判定完成的事项

- 只有静态页面、原型或 mock 数据；
- 只有接口或数据库，没有可用交互和错误恢复；
- 只有单页测试，没有前置依赖与下游消费验证；
- 缺少实现、自动化测试、运行证据或适用发布门禁。
